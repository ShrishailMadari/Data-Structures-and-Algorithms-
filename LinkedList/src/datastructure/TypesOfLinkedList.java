package datastructure;

public class TypesOfLinkedList {
    /*
    * Singly LL: it will store only store next node Address
    * Doubly LL: it will store both next and back node address
    * Circular LL: it will connect the last node to the first node
    * */

    // the last node next node adds is null then its tail node

}

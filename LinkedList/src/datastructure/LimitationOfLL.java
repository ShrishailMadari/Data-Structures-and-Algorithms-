package datastructure;

public class LimitationOfLL {
    public static void main(String[] args) {
        /*adv:
        * does be overcoming the limitation of arrayList
        * 1) it consumes the more memory
        * 2) insertion of node at any place could be done
        * 3) deletion Of Nodes Could be done easily
        * dis-adv:
        * traversing or getting the values from the linked list will be O(n) time
        * */
    }
}
